import React from "react";import{Link}from"react-router-dom";import{ArrowUpRight,ChevronRight,AlertTriangle,CheckCircle2,Clock3,Info,Plus,Filter,Download}from"lucide-react";
export const PageHeader=({eyebrow,title,description,actions})=><div className="page-head"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div><div className="head-actions">{actions}</div></div>;
export const Button=({children,secondary=false,...p})=><button className={secondary?"btn secondary":"btn"} {...p}>{children}</button>;
export const Stat=({label,value,delta,kind=""})=><div className="stat"><span>{label}</span><strong>{value}</strong><small className={kind}>{delta}</small></div>;
export const Status=({children})=>{let c=String(children).toLowerCase();let cls=c.includes("critical")||c.includes("reject")?"red":c.includes("await")||c.includes("proposed")||c.includes("warning")?"amber":c.includes("execut")||c.includes("published")||c.includes("verified")||c.includes("closed")?"green":"blue";return <span className={"status "+cls}>{children}</span>};
export const Priority=({children})=><span className={"priority "+String(children).toLowerCase()}>{children}</span>;
export const Empty=({text})=><div className="empty"><Info size={22}/><p>{text}</p></div>;
export const Section=({title,action,children})=><section className="panel"><div className="panel-head"><h2>{title}</h2>{action}</div>{children}</section>;
export const WorkRow=({w,base,action})=><div className="work-row"><div><b>{w.id}</b><Link to={`${base}/work/${w.id}`}>{w.title}</Link><small>{w.corridor} · {w.section} · {w.asset}</small></div><span>{w.dept}</span><Priority>{w.criticality}</Priority><span>{w.risk}/100</span><Status>{w.status}</Status>{action||<ChevronRight size={16}/>}</div>;
export const Progress=({status})=>{let steps=["Registered","Validated","Prioritized","Window Proposed","Bundled","Plan Proposed","Approved","Published","Executing","Completion Submitted","Verified","Closed"];let i=steps.indexOf(status);return <div className="pipeline">{steps.map((s,n)=><div className={n<=i?"done":""} key={s}><i>{n<=i?"✓":n+1}</i><span>{s}</span></div>)}</div>};
export const Toolbar=({onAdd})=><div className="toolbar"><button><Filter size={15}/> Filters</button><button><Download size={15}/> Export</button>{onAdd&&<Button onClick={onAdd}><Plus size={15}/> Register work</Button>}</div>;
