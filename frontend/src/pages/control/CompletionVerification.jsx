import React from "react";import{useAppStore}from"../../hooks/useAppStore";import{PageHeader,Section,Status,Button}from"../../components/ui/Components";
export default function CompletionVerification(){const{works,updateWork}=useAppStore();let list=works.filter(w=>w.status==="Completion Submitted");return <div className="content"><PageHeader eyebrow="CLOSEOUT CONTROL" title="Completion Verification" description="Field completion is evidence; closure occurs only after supervisory verification."/><Section title="Submitted completion evidence"><div className="verification-list">{list.length?list.map(w=><div className="verify-card" key={w.id}><div><b>{w.id}</b><h3>{w.title}</h3><p>{w.section} · {w.dept}</p></div><div className="evidence"><span>📷 4 photos</span><span>✓ Supervisor note</span><span>✓ Actual duration</span></div><Status>Completion Submitted</Status><div><Button onClick={()=>updateWork(w.id,{status:"Verified"})}>Verify & close</Button></div></div>):<p className="muted">No completion submissions awaiting verification.</p>}</div></Section></div>}
