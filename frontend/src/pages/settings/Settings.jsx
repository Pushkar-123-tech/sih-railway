import PageHeader from "../../components/common/PageHeader";
export default function Settings(){return <div><PageHeader title="System Configuration" subtitle="Planning engine and operational display configuration."/><div className="grid-2"><div className="card"><h3>Optimization objectives</h3>{["Minimize asset downtime","Maximize infrastructure availability","Minimize train conflicts","Maximize multi-department coordination"].map((x,i)=><label className="switch-row" key={x}><span>{x}</span><input type="checkbox" defaultChecked={i!==3}/></label>)}</div><div className="card"><h3>Planning constraints</h3>{["Safety-critical tasks cannot be deferred","Passenger timetable is hard constraint","Goods forecast is soft constraint","Minimum block duration: 60 min","Maximum block duration: 180 min"].map(x=><div className="setting-row" key={x}><CheckMark/><span>{x}</span></div>)}</div></div></div>}
function CheckMark(){return <span className="status-dot"/>}