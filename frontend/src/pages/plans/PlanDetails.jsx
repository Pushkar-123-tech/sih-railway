import { useParams, Link } from "react-router-dom"; import PageHeader from "../../components/common/PageHeader"; import { plans } from "../../data/mockData";
export default function PlanDetails(){const {id}=useParams();const p=plans.find(x=>x.id===id)||plans[0];return <div><PageHeader title={p.id} subtitle={`${p.corridor} · ${p.date} · ${p.start}–${p.end}`}/><div className="grid-2"><div className="card"><h3>Block details</h3><div className="detail-grid"><span>Corridor<b>{p.corridor}</b></span><span>Date<b>{p.date}</b></span><span>Window<b>{p.start}–{p.end}</b></span><span>Tasks<b>{p.tasks}</b></span><span>Status<b>{p.status}</b></span><span>Safety<b>{p.safety}</b></span></div></div><div className="card"><h3>Departments</h3>{p.departments.map(d=><div className="department-row" key={d}><span className="status-dot"/><b>{d}</b><span>Included</span></div>)}<div className="success-box"><b>No train conflicts detected.</b><span>COA timetable and goods forecast checks passed.</span></div><Link className="primary button-link" to="/safety">Open safety validation</Link></div></div></div>;}