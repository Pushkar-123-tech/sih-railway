import React from "react";import{useAppStore}from"../../hooks/useAppStore";import{PageHeader,Section,Status,Button}from"../../components/ui/Components";
export default function Execution({role}){const{works,updateWork}=useAppStore();let list=works.filter(w=>(w.assignedTo==="demo-field"||w.assigned_to==="demo-field")&&["Published","Executing","Completion Submitted"].includes(w.status));return <div className="content"><PageHeader eyebrow="FIELD OPERATIONS" title="Execution Board" description="Allocated work, live execution state, time limits and completion evidence."/><Section title="Today's allocated work"><div className="execution-list">{list.map(w=><div className="exec-card" key={w.id}><div className="exec-time"><b>{w.scheduledStart&&w.scheduledEnd?`${w.scheduledStart}–${w.scheduledEnd}`:w.preferred}</b><small>{w.scheduledDate||"Date pending"} · {w.section}</small></div><div><b>{w.id}</b><h3>{w.title}</h3><p>{w.dept} · Asset {w.allocatedAsset||w.asset} · Crew {w.crew} · Limit {w.durationLimit||w.duration} min</p></div><Status>{w.status}</Status><div className="exec-actions">{w.status==="Published"&&<Button onClick={()=>updateWork(w.id,{status:"Executing"})}>Start work</Button>}{w.status==="Executing"&&<Button onClick={()=>updateWork(w.id,{status:"Completion Submitted"})}>Submit completion</Button>}{w.status==="Completion Submitted"&&<span className="muted">Awaiting verification</span>}</div></div>)}</div></Section><Section title="Completion evidence checklist"><div className="checks"><span>📷 Before / after photographs</span><span>📝 Completion notes</span><span>⏱ Actual start / end time</span><span>🦺 Safety release confirmation</span><span>👤 Supervisor acknowledgement</span></div></Section></div>}
